{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "phonebook api",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "test",
    "build:ui": "del /S /Q dist && cd ..\\phonebook-frontend && npm run build && xcopy dist ..\\phonebook-backend\\dist /E /I /Y",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",    
    "logs:prod": "fly logs"
  },
  "author": "magnus karlberg",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
